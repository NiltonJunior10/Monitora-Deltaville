{
  "name": "monitora-deltaville",
  "private": true,
  "version": "5.2.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check public/app.js && node --check public/service-worker.js"
  },
  "devDependencies": {
    "wrangler": "4.134.0"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
